[tool.pylint.main]
disable = ["all"]
ignore-paths = ["^\\.venv/.*$", "^build/.*$", "^dist/.*$", "^bkp/.*$", "^exemplos/.*$"]

[tool.pylint."messages control"]
enable = ["C0302"]

[tool.pylint.format]
max-module-lines = 350